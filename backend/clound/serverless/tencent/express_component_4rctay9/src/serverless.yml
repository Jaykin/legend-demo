component: express
name: express-Js6Qjv_ZH
org: '1300968839'
app: frontend-demo
stage: dev
inputs:
  src:
    src: ./
    exclude:
      - .env
      - node_modules/**
  region: ap-guangzhou
  runtime: Nodejs10.15
  apigatewayConf:
    protocols:
      - http
      - https
    environment: release
    serviceTimeout: 60
    autoCreateDns: false
  isAutoCiDeploy: false
  functionConf:
    eip: false
    timeout: 3
    memorySize: 128
  layers:
    - name: '${output:${stage}:${app}:frontend-demo-layer.name}'
      version: '${output:${stage}:${app}:frontend-demo-layer.version}'
