package com.jay.profile;

public class RoleTest extends Role {
    public RoleTest() {}

    public RoleTest(String roleName , String note) {
        super.setRoleName(roleName);
        super.setNote(note);
    }
}
