/**
 * 面向接口编程
 * 
 * 接口概念
 *  1、api：一个库或模块对外提供的接口，通过暴露的接口来通信，可以隐藏内部的工作细节
 *  2、interface：语言提供的关键字，如：Java 的 Interface，用来产生一个抽象类，表示一种契约（即创建规则，如必须包含xxx方法），负责建立具体类与具体类的联系
 *  3、【接口是对象能响应的请求的集合】：请求即属性访问
 */